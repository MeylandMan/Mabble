if(NOT DEFINED MABBLE_LIBRARY_TYPE OR MABBLE_LIBRARY_TYPE STREQUAL "")
    if(BUILD_SHARED_LIBS)
        set(MABBLE_LIBRARY_TYPE "SHARED")
    else()
        set(MABBLE_LIBRARY_TYPE "STATIC")
    endif()
endif()